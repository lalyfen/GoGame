# =============================================================================
# GoGame Backend 环境变量配置模板
# =============================================================================
# 复制此文件为 .env 并根据您的环境修改相应的值
# 注意：请勿将包含真实密码的 .env 文件提交到版本控制系统

# =============================================================================
# 数据库配置 (PostgreSQL)
# =============================================================================
DATABASE_NAME=gogame_db
DATABASE_USER=gogame_user
DATABASE_PASSWORD=your_database_password_here
DATABASE_HOST=postgres
DATABASE_PORT=5432

# =============================================================================
# Redis缓存配置
# =============================================================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=your_redis_password_here

# =============================================================================
# Django核心配置
# =============================================================================
SECRET_KEY=your-django-secret-key-here-change-this-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend

# =============================================================================
# 生产环境配置
# =============================================================================
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=your_admin_password_here

# =============================================================================
# Gunicorn配置
# =============================================================================
GUNICORN_WORKERS=1
GUNICORN_THREADS=4
LOG_LEVEL=info

# =============================================================================
# CORS配置
# =============================================================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============================================================================
# 可选配置
# =============================================================================
# 以下配置项有默认值，可以根据需要自定义

# 静态文件配置
# STATIC_URL=/static/
# MEDIA_URL=/media/

# 会话配置
# SESSION_COOKIE_SECURE=False
# CSRF_COOKIE_SECURE=False

# 邮件配置（如需要）
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_email_password

# 缓存超时配置（秒）
# CACHE_TIMEOUT=300

# JWT配置
# ACCESS_TOKEN_LIFETIME=300
# REFRESH_TOKEN_LIFETIME=86400

# =============================================================================
# 配置说明
# =============================================================================
# 1. DATABASE_PASSWORD: 请使用强密码，至少12位，包含大小写字母、数字和特殊字符
# 2. REDIS_PASSWORD: 生产环境建议设置密码
# 3. SECRET_KEY: 生产环境请使用随机生成的密钥，使用以下命令生成：
#    python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# 4. DJANGO_SUPERUSER_PASSWORD: 生产环境请使用强密码
# 5. DEBUG: 生产环境必须设置为 False
# 6. ALLOWED_HOSTS: 生产环境请设置为您的实际域名
# 7. CORS_ALLOWED_ORIGINS: 生产环境请设置为您的实际前端域名

# =============================================================================
# 生产环境安全配置示例
# =============================================================================
# DATABASE_PASSWORD=MySecureP@ssw0rd!2024
# REDIS_PASSWORD=MyRedisP@ssw0rd!2024
# SECRET_KEY=your-50-character-random-secret-key-here
# DJANGO_SUPERUSER_PASSWORD=YourSecureP@ssw0rd!2024
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,api.yourdomain.com
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com